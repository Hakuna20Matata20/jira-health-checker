# 🤖 Jira Health Checker (AI Powered)

Аналітичний інструмент на базі **Streamlit** для проведення аудиту бэклогу Jira. Допомагає виявляти застарілі задачі, перевантаження команди та загальні проблеми з гігієною задач за допомогою інтелектуального мапінгу статусів (через OpenAI API).

## 🌟 Основні можливості
- **Аналіз бэклогу з двох джерел**:
  - Завантаження класичних експортів з Jira (CSV / Excel).
  - Пряме підключення до Jira API для отримання найактуальніших даних.
- **AI-класифікація статусів**: Автоматичне визначення категорій (`TODO`, `IN_PROGRESS`, `DONE`) для будь-яких кастомних статусів Jira за допомогою OpenAI.
- **Розрахунок ключових метрик здоров'я**:
  - 🧟 **Зомбі задачі**: Задачі "в роботі" або "до виконання", які не оновлювалися понад X днів.
  - 👻 **Орфани (Orphans)**: Задачі без призначеного виконавця.
  - 💀 **Мертві Епіки (Dead Epics)**: Незавершені епіки без рухів понад 30 днів.
  - 🏋️ **Перевантаження**: Виявлення членів команди, які мають більше 2 активних задач одночасно.
  - 📉 **Оцінка здоров'я (Health Score)**: Комплексний показник якості бэклогу (від 0 до 100).
- **Візуальна аналітика**: Графіки розподілу задач та навантаження на команду за допомогою Plotly.

---

## 🚀 Встановлення та запуск

### 1. Клонування репозиторію
```bash
git clone https://github.com/Hakuna20Matata20/jira-health-checker.git
cd jira-health-checker
```

### 2. Встановлення залежностей
Рекомендовано використовувати віртуальне середовище (`venv` / `conda`).
```bash
python -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 3. Запуск застосунку
```bash
streamlit run app.py
```
Після запуску відкриється вікно браузера з адресою `http://localhost:8501`.

---

## 📖 Як користуватися

Ви можете знайти більш детальну інформацію про роботу з додатком у вкладці **"Документація (Help)"** безпосередньо в інтерфейсі застосунку, або звернувшись до файлу `DOCUMENTATION.md`.

Короткий алгоритм:
1. Запустіть застосунок.
2. (Опціонально) Введіть **OpenAI API Key** у бічній панелі для автоматичної класифікації статусів.
3. Оберіть джерело даних: **CSV Upload** або **Jira API** та надайте необхідні дані (файли або токен доступу).
4. Перевірте та відкоригуйте (за потреби) мапінг статусів на вкладці **"Налаштування статусів"**.
5. Аналізуйте результати на вкладках **"Загальний огляд"**, **"Візуальна аналітика"** та переглядайте конкретні проблемні тікети в **"Задачі до розгляду"**.

---

## 🧪 Тестування

Проєкт покритий Unit та Integration тестами за допомогою `pytest`.
Детальний опис стратегії тестування є у `DOCUMENTATION.md`.

Для запуску всіх тестів:
```bash
pytest test_suite.py -v
```
Для запуску тільки unit-тестів (без підключення до реальної Jira):
```bash
pytest test_suite.py -v -k "not real_jira"
```
